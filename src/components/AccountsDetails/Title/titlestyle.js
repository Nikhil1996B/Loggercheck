export const titleStyle = {
    Card: (sm) => ({
        background: '#ffffff',
        width: '100%',
        border: '0',
        textAlign: `${sm ? 'center' : ''}`
    }),
    title: (sm) => ({
        paddingLeft: '0',
        border: 'none',
        fontSize: '25px',
        fontWeight: 'bold',
        textTransform: 'capitalize'
    })
}